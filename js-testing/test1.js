/*crie um prog que apresenta lista de todas as letras correspondentes aos numeros de 0 ate 255*/

for (let i=0; i <=255; i++){
    console.log(`$(1) = ${String.fromCodePoint}`)
}

let letras = palavra.split("");

let posicao = letras.indexOf(letra);

if (posicao !== -1) {
    console.log("a letra esta na posicao ", posicao);
} else {
    console.log("a ltra nao foi encontrada");
}
