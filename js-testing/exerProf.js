/* dada uma palavra e uma letra, verificasse se essa letra esta na palavra e dizer A posicao dela */

let palavra = prompt("insira a palavra:");
let letra = prompt("insere a letra que deseja encontrar:");

let posicoes = [];

for (let i = 0; i < palavra.length; i++) {
  if (palavra[i] === letra) {

  }
}
